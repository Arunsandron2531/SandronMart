<%- include('../partials/header', { title: 'Your Cart' }) %>

<div class="container">
  <div class="page-head">
    <div>
      <h1>Your Cart</h1>
      <p class="muted">Review the items in your shopping basket.</p>
    </div>
  </div>

  <% if (messages.success) { %>
    <div class="alert alert-success"><%= messages.success %></div>
  <% } %>
  <% if (messages.error) { %>
    <div class="alert alert-error"><%= messages.error %></div>
  <% } %>

  <% if (items.length === 0) { %>
    <div class="empty-state">
      <div class="empty-icon">🛒</div>
      <h3>Your cart is empty</h3>
      <p>Your cart is empty. Add some plants and gardening supplies to get started.</p>
      <a class="btn btn-primary" href="/buyer/products">Continue Shopping</a>
    </div>
  <% } else { %>
    <div class="cart-list">
      <% items.forEach(function (item) { %>
        <div class="cart-item">
          <div class="cart-thumb">
            <% if (item.image_url) { %>
              <img class="cart-image" src="<%= item.image_url %>" alt="<%= item.name %>"
                   loading="lazy" onerror="this.style.display='none';this.nextElementSibling.style.display='grid';">
              <div class="cart-image cart-image-fallback" style="display:none;">🌱</div>
            <% } else { %>
              <div class="cart-image cart-image-fallback">🌱</div>
            <% } %>
          </div>
          <div class="cart-info">
            <span class="product-category"><%= item.category %></span>
            <h3 class="product-name"><%= item.name %></h3>
            <div class="cart-unit">$<%= Number(item.price).toFixed(2) %> each</div>
          </div>
          <div class="cart-qty">
            <form class="qty-form" method="post" action="/buyer/cart/<%= item.cart_id %>/update">
              <input type="hidden" name="action" value="decrease">
              <button class="qty-btn" type="submit" aria-label="Decrease quantity">&minus;</button>
            </form>
            <span class="qty-value"><%= item.quantity %></span>
            <form class="qty-form" method="post" action="/buyer/cart/<%= item.cart_id %>/update">
              <input type="hidden" name="action" value="increase">
              <button class="qty-btn" type="submit" aria-label="Increase quantity">+</button>
            </form>
          </div>
          <div class="cart-subtotal">$<%= Number(item.price * item.quantity).toFixed(2) %></div>
          <form class="cart-remove" method="post" action="/buyer/cart/<%= item.cart_id %>/remove">
            <button class="btn btn-danger-outline btn-sm" type="submit">Remove</button>
          </form>
        </div>
      <% }) %>
    </div>

    <div class="cart-summary">
      <div class="cart-total">Total: <strong>$<%= total.toFixed(2) %></strong></div>
      <div class="cart-summary-actions">
        <a class="btn btn-outline" href="/buyer/products">Continue Shopping</a>
        <button class="btn btn-primary" type="button" disabled title="Checkout coming soon">Checkout</button>
      </div>
    </div>
  <% } %>
</div>

<%- include('../partials/footer') %>